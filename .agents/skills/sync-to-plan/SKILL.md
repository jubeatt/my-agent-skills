---
name: sync-to-plan
description: 把某段開發的實際實作回寫同步到 plan 檔（預設 .plan/task.md）：以 git diff 為真相來源就地校正整份 plan，而非在結尾追加進度。適用於「同步 plan」「回寫 task.md」，或 dev 段落完成後要讓計畫反映現況。
---

# Skill: Sync to Plan

以**實作為準，回頭校正整份 plan**，讓它隨時是一份乾淨、內部一致的計畫。重點不是「補一段進度」，
而是拿 git 的實際結果去修正 plan 的前後文。

## 核心原則

1. **diff 是「做了什麼」的真相來源。** 哪些 plan 條目該標完成／改寫，一律看 git diff，不憑印象——
   使用者可能在別的 session／用其他工具／手動改過。當前對話若有相關討論，用來理解「為什麼這樣改」、
   幫助改寫描述，但不取代 diff。
2. **就地校正，不是追加。** 該標完成的標完成、該刪的整段刪、該改寫的直接改寫，讓改完的 plan 好讀，
   而不是在結尾疊一段「更新紀錄」。
3. **順著 plan 原本的結構與標記慣例改，不套自己的模板。**
4. **只寫 plan 檔。** 對程式碼與 git 唯讀：不改 source、不 commit、不跑 build。
5. **有根據才改，不確定就問。** 每處修改都要對得上 diff／檔案證據；若某個改動看不出意圖、
   以致無法判斷 plan 該怎麼調，先問使用者，不要臆測或硬改。

## 流程

### 1. 定位 plan 檔

預設 `.plan/task.md`，**直接從磁碟讀**——plan 常是 untracked 或有未提交手改，磁碟上的才是使用者眼前的版本。
讀不到就**中止並請使用者指出位置**，不要自行猜測或改別的檔。

### 2. 從 git 讀出這段做了什麼

預設看最新一筆 commit：先 `git status` + `git log --oneline -n 5` 確認 HEAD 就是要同步的那筆，再 `git show HEAD`。
使用者若指定別的 commit 就用指定的。有相關未提交變更則一併看 `git diff HEAD`，並在回報時點明。

### 3. 比對 plan 宣稱 vs 實際實作

逐項分類：

- **已完成**：依該 plan 慣例標完成或整段移除。
- **有偏差**：做法與原規劃不同 → 改寫描述，並修掉因此**過期或自相矛盾**的敘述
  （例如原本寫「避免 X」但實際用了 X，那句就要改掉）。
- **已失效**：不再成立的段落／檔名／假設 → 移除或改寫。
- **仍未做**：維持原樣，別誤標成完成。

### 4. 就地改寫

- 做字串替換前先 `grep` 出目標行的**精確原文**（縮排／空白／全形符號常和印象不同），替換失敗就重讀該區塊拿正確字串。
- 刪整段時顧好前後銜接——分隔線、空行、章節編號別刪出破洞。

### 5. 一致性檢查

改完不只看動過的地方：

- **交叉引用**：`grep` 剛移除／改名的東西，確認沒有殘留的失效指涉；刻意保留的回溯引用（如「原 Task 8」）要讀者看得懂。
- **編號／順序**：移除某段後，檢查是否影響後續編號或「放最後」之類的相對描述。
- **可讀性**：通讀改動區塊，確認讀起來仍像一份完整計畫。

### 6. 回報

摘要同步了什麼：哪些標完成／移除、哪些描述被改寫、有無刻意保留的引用、有無因未提交變更或改動意圖不明需使用者確認的地方。
